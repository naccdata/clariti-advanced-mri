[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "qsm-medi"
version = "2.5.0"
description = "A Python package for QSM processing and analysis."
requires-python = ">=3.9"
dependencies = [
    "nibabel",
    "flywheel-sdk",
    "fw-client",
    "hd-bet @ file:///opt/HD-BET",
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.1.0",
]

[tool.ruff]
line-length = 88
indent-width = 4
target-version = "py311"
extend-exclude = ["src/hd-bet","src/mSMV-vkernel_250813","qsm_optimization","archive"]

[tool.ruff.lint]
select = ["A", "B", "E", "W", "F", "I", "RUF", "SIM", "C90", "PLW0406", "COM818", "SLF001"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = [
    "--cov=image_uploading",
    "--cov=qsmxt",
    "--cov-report=html",
    "--cov-report=term-missing"
]

[tool.hatch.build.targets.wheel]
      packages = ["build"]
